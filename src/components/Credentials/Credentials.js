
import React from 'react';
import { Thumbnail } from 'react-bootstrap';

import Credentials0 from './credentials0.png';

const cred = { padding: '1.5%' };

const Credentials = (
  <Thumbnail href="#" alt="171x180" src={Credentials0} style={cred} />
);

export default Credentials;
